apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bracket.fullname" . }}-test-connection"
  labels:
    {{- include "bracket.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bracket.fullname" . }}:{{ .Values.frontend.service.port }}']
  restartPolicy: Never
