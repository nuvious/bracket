apiVersion: v1
kind: Service
metadata:
  name: {{ include "bracket.fullname" . }}
  labels:
    {{- include "bracket.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bracket.selectorLabels" . | nindent 4 }}
